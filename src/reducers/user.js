export function user (state = {}, action) {
    switch(action.type) {
        case 'SOCIAL_LOGIN':
            debugger;
            break;
        default:
            return state;
    }

    return state;
}
